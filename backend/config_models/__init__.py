# config_models — pure-Python dataclass models for D3 multi-faculty configuration.
# These are NOT SQLAlchemy models. They do not touch the DB.
# They live here to avoid a naming conflict with the flat models.py SQLAlchemy file.
